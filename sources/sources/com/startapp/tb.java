package com.startapp;

import android.webkit.WebView;
import java.lang.ref.WeakReference;

/* compiled from: Sta */
public class tb extends WeakReference<WebView> {
    public tb(WebView webView) {
        super(webView);
    }
}
